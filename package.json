{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r --if-present run build || true"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@types/node": "^20.0.0"
  }
}
